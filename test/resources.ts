import * as Path from "path";

export const mockedIDEPackagePath = Path.normalize(Path.join(__dirname, "../../test/resources/ArduinoIDE/hardware"));
export const mockedIDELibPath = Path.normalize(Path.join(__dirname, "../../test/resources/ArduinoIDE/libraries"));
export const mockedIDEExamplesPath = Path.normalize(Path.join(__dirname, "../../test/resources/ArduinoIDE/examples"));
export const mockedPackagePath = Path.normalize(Path.join(__dirname, "../../test/resources/Arduino15"));
export const mockedSketchbookPath = Path.normalize(Path.join(__dirname, "../../test/resources/Documents/Arduino"));
